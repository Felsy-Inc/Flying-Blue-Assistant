import type { AlertMatchDispatchInput } from './types'

/** Placeholder for Web Push / native push (Phase N). */
export async function dispatchPushAlertMatch(_input: AlertMatchDispatchInput): Promise<void> {
  /* intentionally empty */
}
